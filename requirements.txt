# OniQuant v6.0 — High-Performance ASGI Ingestion Stack
# Pinned for deterministic Railway.app builds

# --- Core ASGI Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0      # Includes uvloop + httptools as extras

# --- Performance-Critical Dependencies ---
uvloop==0.21.0                 # libuv-based event loop (2-4x asyncio throughput)
httptools==0.6.4               # llhttp-based HTTP parsing (replaces h11)
orjson==3.10.13                # SIMD-accelerated JSON (de)serialization

# --- Redis Streams (Message Broker) ---
redis[hiredis]==5.2.1          # hiredis C parser for wire-protocol acceleration

# --- Observability ---
structlog==24.4.0              # Structured async-safe logging

# --- Broker Connectors ---
ib_async==1.0.3                # IBKR Gateway connector (async ib_insync fork)
pybit==5.9.0                   # Bybit Unified V5 REST + WebSocket

# --- WFO / Backtesting Engine ---
vectorbt==0.26.2               # Vectorized backtesting (NumPy/Pandas core)
numpy==1.26.4                  # Pinned for vectorbt compat
pandas==2.2.3                  # DataFrame engine

# --- Runtime ---
python-dotenv==1.0.1           # Railway env injection fallback
